package com.zurrtum.create;

import net.fabricmc.api.ModInitializer;

public class OptimizationOnly implements ModInitializer {
    @Override
    public void onInitialize() {
        // Intentionally empty: Create content is disabled in optimization-only mode.
    }
}
